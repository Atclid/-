name: 构建模组
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 配置 JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: 清除旧缓存
        run: rm -rf ~/.gradle/caches/

      - name: 生成 Gradle Wrapper（使用 7.5 版本，与 6.0.14 兼容）
        working-directory: bonsaitree_mod/bonsaitree_forge_mod
        run: |
          gradle wrapper --gradle-version=7.5 --distribution-type=bin
          chmod +x gradlew

      - name: 构建（带重试）
        working-directory: bonsaitree_mod/bonsaitree_forge_mod
        run: |
          for i in {1..3}; do
            ./gradlew build --stacktrace --no-daemon && exit 0
            echo "第 $i 次失败，重试..."
            sleep 5
          done
          exit 1

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: 模组产物
          path: bonsaitree_mod/bonsaitree_forge_mod/build/libs/*.jar
